<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.layer.xdk.ui.message.audio.AudioMessageModel"/>

        <variable
            name="messageModel"
            type="AudioMessageModel"/>
    </data>

    <com.layer.xdk.ui.message.audio.AudioMessageLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:messageModel="@{messageModel}">

        <ImageView
            android:id="@+id/preview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:adjustViewBounds="true"
            android:maxHeight="@dimen/xdk_ui_audio_message_maximum_height"
            android:maxWidth="@dimen/xdk_ui_message_default_max_width"
            android:scaleType="centerCrop"
            app:loadFrom="@{messageModel.previewRequestParameters}"
            app:usingImageLoader="@{messageModel.imageCacheWrapper}"/>

        <ProgressBar
            android:id="@+id/progress_bar"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="@dimen/xdk_ui_audio_message_progress_bar_height"
            android:minHeight="0dp"
            android:visibility="gone"/>

    </com.layer.xdk.ui.message.audio.AudioMessageLayout>

</layout>